import Styles from '../styles/HeroSection.module.css';

export default function HeroSection() {
    return (
        <div className={Styles.heroSection}>
            <h4 className={Styles.heroSectionTitle}>Education</h4>
        </div>
    )
}
